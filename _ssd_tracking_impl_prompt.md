You are implementing a new feature in Monitor Island, a single-binary macOS menu-bar app (Apple Silicon arm64, macOS 14+) at /Users/sean/Downloads/files. It shows a floating "island" of live chip metrics (CPU, mem, GPU, temp, power, network) and which AI workloads are running. You will ADD: real-time SSD write tracking, a cumulative-over-time host-bytes-written total that survives reboots, a derived best-effort "perceived SSD damage %", per-process write attribution naming Claude Code and Codex as first-class culprits, and a low-frequency append-only damage log. Use a FLEET OF SONNET SUBAGENTS to parallelize, with rigorous self-testing (the binary is its own test harness — there is no unit-test target).

=== THE GROUNDED PROBLEM ===
Agentic AI coding tools can silently hammer an SSD. Real case — OpenAI Codex issue #28224: a TRACE-logging bug wrote ~37 TB to ~/.codex/logs_2.sqlite over 21 days (~640 TB/year ≈ ~640 full-drive writes/year on a 1 TB SSD). Consumer NVMe drives have finite write endurance (TBW). Users have no visibility into how hard these tools beat on their SSD. We want to SEE it in real time, watch it accumulate, and keep a damage log — with Claude Code and Codex attributed.

=== HARD CONSTRAINTS (NON-NEGOTIABLE — from the project's CLAUDE.md) ===
1. SUDOLESS OR IT DOESN'T SHIP. Every metric must come from an API needing no elevated privilege and no permission prompt. NEVER powermetrics, sudo, fs_usage, or anything triggering a password/TCC prompt.
2. APPLE SILICON ONLY. No Intel fallbacks.
3. HONESTY ABOUT ACCURACY IS A FEATURE. Estimates must be flagged as estimates. The project already does this: `tempBestEffort: Bool` and `aneEstimateNote: String?` in Snapshot.swift. Your wear estimate MUST follow that exact precedent (a `diskWearBestEffort` flag + a `diskWearNote` string + a "~"/"est" UI marker). Never fabricate a precise-looking hardware number.
4. THE DAMAGE LOG MUST NOT ITSELF WEAR THE SSD (that would be ironic and self-defeating). Append-only, in-memory accumulation, flushed at most once every 5 minutes, plain JSON-lines (NEVER SQLite/WAL). Enforce with a code-level `precondition(flushInterval >= 60)`.
5. STRICT 3-COLOR THEME (sky/snow/slate) + one customizable accent. Add NO new colors. Escalation may reuse the already-allowed `Theme.pressureWarn` (amber) / `Theme.pressureCritical` (red), which are the documented SWAP exception.

=== THE PIPELINE YOU MUST SLOT INTO (one direction, one Snapshot per tick) ===
per-metric samplers -> Sampler.tick() (holds prev state, computes deltas) -> Snapshot (Codable, --dump output) -> IslandModel (@Published, bg DispatchSourceTimer at 1/2/5 s) -> Smoother (60 Hz lerp to targets; owns history rings; view reads SMOOTHED values) -> IslandView (SwiftUI: compact pill <-> expanded card, ring gauges, sparkline).

Key files under /Users/sean/Downloads/files/Sources/MonitorIsland/ :
- Network.swift — THE TEMPLATE for a delta-based throughput sampler (prevRx/prevTx UInt64, primed flag, Date elapsed, `&-` wrapping subtraction, `max(0, …)` clamp). Disk.swift mirrors this exactly.
- GPU.swift — THE TEMPLATE for the public-IOKit enumeration idiom: `IOServiceMatching("IOAccelerator")`, `IOServiceGetMatchingServices(kIOMainPortDefault, …)`, iterate with `IOIteratorNext` + `defer { IOObjectRelease(service); service = IOIteratorNext(iterator) }`, `IORegistryEntryCreateCFProperties(service, &props, kCFAllocatorDefault, 0)`, `props?.takeRetainedValue() as? [String: Any]`. Copy this shape; use `kIOMainPortDefault` (NOT the deprecated kIOMasterPortDefault).
- Snapshot.swift — the single Codable struct the whole app passes around AND what --dump serializes. A new metric = new fields here. `round1`/`round2` helpers live here.
- Sampler.swift — single owner of all samplers + the only holder of previous state. `prime()` is called once before the first real `tick()` for delta samplers. `tick()` builds the Snapshot. Already owns `private let workloads = WorkloadSampler()`.
- Workloads.swift — enumerates all PIDs sudoless and detects "Claude Code" (~line 145) and "Codex" (~line 154). CRUCIAL: `residentMB(for:)` (~line 73) ALREADY calls `proc_pid_rusage(pid, RUSAGE_INFO_V4, &info)` with a `rusage_info_v4` struct and reads `info.ri_resident_size`. That SAME struct contains `ri_diskio_byteswritten` and `ri_proc_start_abstime` — per-process disk I/O, sudoless for same-user processes, at ZERO extra syscall cost.
- Smoothing.swift (Smoother) — 60 Hz @MainActor lerp (`displayed += (target - displayed) * 0.18`). Owns `gpuHistory: [Double]` (cap 60, pushed in setTargets at tick cadence, NOT in frame()). Net rates use a `/1000.0` gap scaling. Has `setTargets(from:)`, `snapToTargets()`, `frame()`, `step()`.
- IslandView.swift — the SwiftUI island. `expandedContent` (~line 387) is a VStack with a 4-ring HStack then detail rows via `row(_:_:trailing:warn:)`, then the GPU ACTIVITY `Sparkline`, then the workloads section with `workloadGroup(_:)`. Helpers: `fmtRate(_:)`, `fmtMem(_:)`. The compact pill (`compact`, ~line 313) has 4 slots (GPU/MEM/CPU/SWAP) + a chevron and is FULL — DO NOT touch it.
- Theme.swift — the palette. `Theme.pressureWarn`, `Theme.pressureCritical`, `Theme.textFaint`, `Theme.textPrimary`, `Theme.textSecondary`, `.brand(size,weight:)`, `.mono(size,weight:)`.
- main.swift — arg parsing; `runDump()`, `runSensors()`. --dump primes, usleeps 400 ms, ticks once, prints JSON. --sensors enumerates HID/IOReport.
- Shot.swift — renders the live expanded card to a PNG via ImageRenderer; drives `smoother.step()` then `snapToTargets()` before capture.
- AppDelegate.swift — has `applicationDidFinishLaunching`; does NOT yet have `applicationWillTerminate`.
- Package.swift — already links IOKit. NO changes needed. Do NOT add a CIOReport shim — everything here is public IOKit + Foundation.

=== ALREADY-VERIFIED FACTS (do not re-litigate; you may re-confirm) ===
- `ioreg -c IOBlockStorageDriver -r -w 0 | grep 'Bytes (Write)'` returns, sudoless (uid 501), exactly ONE non-zero writer (the internal SSD, ~570985172992 bytes ≈ 531 GB) and five virtual/disk-image entries at 0. Keys are exactly `"Bytes (Write)"` and `"Bytes (Read)"`, OSNumber values nested under the top-level `"Statistics"` dict. SUMMING all entries equals the internal-SSD total (the zeros don't contribute) — so no IORegistry parent-walking is needed.
- The IOBlockStorageDriver counters RESET ON REBOOT.
- In the SDK header /Library/Developer/CommandLineTools/SDKs/MacOSX.sdk/usr/include/sys/resource.h: `struct rusage_info_v4` (line ~289) contains `ri_proc_start_abstime` (line ~299), `ri_diskio_bytesread` (~307), `ri_diskio_byteswritten` (~308). `RUSAGE_INFO_V4 = 4`. `ri_logical_writes` is NOT in v4 (it starts in v5) — so use `ri_diskio_byteswritten` (also the correct block-layer wear field).
- The app is NON-sandboxed (Package.swift adds only `-undefined dynamic_lookup`), so `~/Library/Application Support/MonitorIsland/` is writable with no prompt. It does not exist yet — first-run create is required.
- Real attribution targets exist on this machine: `~/.codex/logs_2.sqlite` (9.3 MB) and `~/.claude/projects`.
- Baseline `swift build` is GREEN.

=== WHAT TO BUILD (the merged design) ===
A) NEW FILE Sources/MonitorIsland/Disk.swift with three small types:
   1. `DiskSampler` — clone of NetworkSampler. State: `prevWrite/prevRead: UInt64`, `prevTime: Date`, `primed: Bool`, `lifetimeBaseline: UInt64` (seeded in init from `DamageLogger.readLastLifetimeBytes()`), `sessionWritten: UInt64`. `func sample() -> (writeBps: Double, readBps: Double, sessionWrittenBytes: UInt64, lifetimeWrittenBytes: UInt64)`: enumerate `IOServiceMatching("IOBlockStorageDriver")` using GPU.swift's exact iterator+defer idiom; for each service read `["Statistics"]` then sum `(stats["Bytes (Write)"] as? NSNumber)?.uint64Value` and `Bytes (Read)` with `&+=`. Rate math IDENTICAL to Network.swift (`elapsed = max(0.001, now.timeIntervalSince(prevTime))`, `Double(cur &- prev) / elapsed`, `max(0, …)`). Accumulate `sessionWritten += (curWrite &- prevWrite)` only when primed. `lifetimeWritten = lifetimeBaseline + sessionWritten`.
   2. `SSDWear` — pure helper. `ratedTBW(forCapacityBytes:) -> Double` mapping nearest tier {256GB→300, 512GB→600, 1TB→1000, 2TB→2000}, default 1000. `damagePercent(lifetimeBytes:, ratedTBW:) -> Double = lifetimeBytes / (ratedTBW*1e12) * 100`. `note(ratedTBW:) -> String` = "Apple does not publish TBW; estimate = host bytes / ~<N> TB rated; actual NAND wear is higher (write amplification)." `capacityBytes() -> UInt64` reads the AppleANS controller "Controller Characteristics" → "capacity" sudoless via IOKit (fallback 1_000_000_000_000). If reading capacity proves fiddly, just return the fallback — do not block on it.
   3. `DamageLogger` — `url = FileManager.default.urls(for: .applicationSupportDirectory, in: .userDomainMask).first! + "MonitorIsland" + "disk_damage.jsonl"`, `createDirectory(withIntermediateDirectories: true)` on init. `let flushInterval: TimeInterval = 300` with `precondition(flushInterval >= 60)`. `maybeAppend(sessionWrittenGB:lifetimeWrittenGB:damagePct:claudeCodeMB:codexMB:)` guarded by `lastFlush`; appends one JSON-lines record via `FileHandle(forWritingTo:).seekToEndOfFile()+write` (fallback `data.write(.atomic)` if absent). `flushNow()` for the quit hook. `readLastLifetimeBytes() -> UInt64` seeks the file tail (read last ~2 KB, find last newline, parse `lifetimeWrittenGB`, ×1e9); missing/corrupt → 0. NEVER SQLite. Record schema: `{"ts":ISO8601,"sessionWrittenGB":Double,"lifetimeWrittenGB":Double,"damagePct":Double,"claudeCodeMB":Double?,"codexMB":Double?}` (omit claude/codex when 0).

B) Snapshot.swift — after the `// Network` section (line ~72) add a `// Disk` section: `diskWriteBytesPerSec: Double = 0`, `diskReadBytesPerSec: Double = 0`, `diskSessionWrittenGB: Double = 0`, `diskLifetimeWrittenGB: Double = 0`, `diskWearPercent: Double = 0`, `diskWearBestEffort: Bool = true`, `diskTBWAssumed: Double = 1000`, `diskWearNote: String? = nil`. Add `var diskWrittenSessionMB: Double = 0` to BOTH `WorkloadEntry` (line ~23) and `WorkloadInstance` (line ~17). All default-initialized (Codable + back-compat).

C) Workloads.swift — add to WorkloadSampler: `private var diskBaseline: [Int32: (written: UInt64, startAbs: UInt64)] = [:]`, `private var sessionWritten: [String: UInt64] = [:]`, `private var instWritten: [Int32: UInt64] = [:]`. Add `private func diskWritten(for pid: Int32) -> (written: UInt64, startAbs: UInt64)` copying residentMB's exact `withUnsafeMutablePointer/withMemoryRebound` shape, returning `(info.ri_diskio_byteswritten, info.ri_proc_start_abstime)`, `guard r == 0 else { return (0,0) }`. Where each Claude Code / Codex PID is matched (the add() sites ~lines 148/155), accumulate the delta: `let cur = diskWritten(for: pid); if let base = diskBaseline[pid], base.startAbs == cur.startAbs { let delta = cur.written >= base.written ? cur.written - base.written : 0; sessionWritten[label, default:0] += delta; instWritten[pid] = (instWritten[pid] ?? 0) + delta }; diskBaseline[pid] = cur`. When building entries (~lines 206-211), set `diskWrittenSessionMB` on each WorkloadEntry (`Double(sessionWritten[label] ?? 0)/(1024*1024)`, round2) and on each WorkloadInstance from `instWritten`. Add `func sessionWrittenBytes(forLabel label: String) -> UInt64 { sessionWritten[label] ?? 0 }`. Prune diskBaseline/instWritten for PIDs absent from the current scan.

D) Sampler.swift — add `private let disk = DiskSampler()`, `private let damageLog = DamageLogger()`, and a cached `private lazy var diskCapacityBytes: UInt64 = SSDWear.capacityBytes()`. In `prime()` add `_ = disk.sample()`. MOVE the new Disk block to AFTER the Workloads block (it needs the per-label totals): call `disk.sample()`, set the eight Snapshot disk fields (use round1 for rates, round2 for GB/percent), compute the TBW tier + damage% + note, copy `workloads.sessionWrittenBytes(forLabel:)` into the matching `snap.workloads[i].diskWrittenSessionMB`, then `damageLog.maybeAppend(...)` with the Claude/Codex MB totals.

E) Smoothing.swift — add `@Published var diskWrite: Double = 0`, `diskRead: Double = 0`, `@Published var diskWriteHistory: [Double] = []`, private `tDiskWrite/tDiskRead`. In `setTargets(from:)` set the targets + push `diskWriteHistory.append(s.diskWriteBytesPerSec)` with the same historyCap trim as gpuHistory. In `snapToTargets()` add `diskWrite=tDiskWrite; diskRead=tDiskRead`. In `frame()` add the SAME `/1000.0` gap scaling + lerp as the net lines. (Damage %/cumulative GB are read straight from `snap` — no smoothing, like swapUsedPercent.)

F) IslandView.swift — in `expandedContent` after the Net row (lines ~412-414): (1) `row("Disk", String(format:"\u{2191} %@", fmtRate(s.diskWrite)), trailing: String(format:"\u{2193} %@", fmtRate(s.diskRead)))`; (2) an SSD damage row: `let dmg = snap.diskWearPercent; row("SSD~", String(format:"~%.2f%% est", dmg), trailing: fmtWritten(snap.diskLifetimeWrittenGB), warn: dmg >= 10)`; (3) a WRITE ACTIVITY `Sparkline(data: s.diskWriteHistory, accent: accent)` block mirroring the GPU ACTIVITY block; (4) inside `workloadGroup(_:)` after the memory Text (~line 502), for `w.label.contains("Claude") || w.label.contains("Codex")` with `w.diskWrittenSessionMB > 0`, a faint `Text("wrote \(fmtMem(w.diskWrittenSessionMB))").font(.mono(11)).foregroundStyle(Theme.textFaint)` line. Add `func fmtWritten(_ gb: Double) -> String { gb >= 1000 ? String(format:"%.2f TB life", gb/1000) : String(format:"%.0f GB life", gb) }`. DO NOT touch the compact pill. Use ONLY accent + the existing pressure colors.

G) AppDelegate.swift — add `func applicationWillTerminate(_ notification: Notification)` that flushes the damage log (expose a `flushDamageLog()` path through IslandModel/Sampler, or a small hook). This is a nicety (the 5-min flush bounds loss) — keep it minimal.

H) main.swift (OPTIONAL, recommended) — in `runSensors()` add a "=== Disk (IOBlockStorageDriver Statistics + derived wear) ===" section enumerating every IOBlockStorageDriver, printing each raw Statistics, the non-zero writer, the capacity, the chosen TBW tier, and current damage%. Fold into existing --sensors (no new flag). Defer if time-constrained.

=== HONESTY CONTRACT (must hold) ===
- diskWrite/diskReadBytesPerSec: EXACT, no flag, but HOST bytes to the block layer, not NAND writes (label "host writes", never "NAND").
- diskLifetimeWrittenGB: "since Monitor Island install", NOT a drive odometer (no SMART in v1). A verifiable lower bound.
- diskWearPercent: DERIVED, best-effort; ALWAYS render "~"+"est"; diskWearBestEffort hard-coded true; diskWearNote states the TBW assumption + write-amplification caveat. Apple publishes NO TBW.
- Per-process numbers use ri_diskio_byteswritten (block I/O), NOT ri_logical_writes; "session" scope; same-user only; a lower bound. Leave a one-line `// TODO:` at the damage-computation site noting where a future verified NVMe SMART PERCENTAGE_USED could override the estimate and set diskWearBestEffort=false. DO NOT implement NVMe SMART in v1 (conflicted sudoless feasibility on Apple Silicon + PAC-fragile private vtable call).

=== SUBAGENT FLEET PLAN (parallelize with Sonnet subagents) ===
Run a FIRST WAVE of three independent subagents in parallel (they touch disjoint files):
  • Subagent 1 — "Disk.swift sampler + logger + wear": create Disk.swift (DiskSampler, SSDWear, DamageLogger) per (A). INPUT: Network.swift + GPU.swift as templates; the verified IOKit keys; the JSONL schema. OUTPUT: compiling Disk.swift. ACCEPTANCE: file compiles in isolation against the package; DiskSampler.sample() returns 4 values; precondition(flushInterval>=60) present; no SQLite, no NVMe SMART, no CIOReport.
  • Subagent 2 — "Snapshot fields": apply (B). INPUT: Snapshot.swift current shape; the field list. OUTPUT: edited Snapshot.swift. ACCEPTANCE: all eight disk fields + the two WorkloadEntry/Instance fields added, default-initialized, project still type-checks the struct.
  • Subagent 3 — "Per-process attribution": apply (C) in Workloads.swift. INPUT: the residentMB rusage pattern; the verified rusage_info_v4 fields; the Claude/Codex match sites. OUTPUT: edited Workloads.swift with diskWritten(for:), the per-label accumulator, PID-reuse defense, the accessor, and entry population. ACCEPTANCE: compiles; sessionWrittenBytes(forLabel:) exists; negative-delta + startAbs-change handling present; maps pruned.
Then a SECOND WAVE of three subagents (they depend on wave 1's symbols; run in parallel after wave 1 lands and `swift build` passes):
  • Subagent 4 — "Sampler wiring": apply (D). ACCEPTANCE: disk + damageLog instances added; primed; Disk block AFTER Workloads; all snap fields set; per-label MB copied into workloads entries; maybeAppend called.
  • Subagent 5 — "Smoother": apply (E). ACCEPTANCE: diskWrite/diskRead glide with /1000 scaling; diskWriteHistory pushed in setTargets (tick cadence) and trimmed; both added to snapToTargets.
  • Subagent 6 — "IslandView UI + AppDelegate quit flush": apply (F) and (G). ACCEPTANCE: Disk row, SSD~ row (with ~/est), WRITE ACTIVITY sparkline, per-culprit "wrote N MB" lines; fmtWritten added; compact pill untouched; no new Theme colors; applicationWillTerminate flushes the log.
Optionally Subagent 7 — main.swift --sensors disk section (H).
Each subagent MUST run `swift build` for its own slice where possible and report the exact diff it made. After each wave, the orchestrator runs `swift build` and does not start the next wave until it is GREEN.

=== VERIFICATION (the binary is the test harness — do ALL of this) ===
1. `cd /Users/sean/Downloads/files && swift build` → must be green. Then `swift build -c release`.
2. `swift run MonitorIsland --dump` → confirm the new JSON fields are present and plausible: diskWriteBytesPerSec, diskReadBytesPerSec, diskSessionWrittenGB, diskLifetimeWrittenGB (~hundreds of GB on first run — roughly tracks the live ioreg figure), diskWearPercent (small, e.g. tenths of a %), diskWearBestEffort:true, diskTBWAssumed, diskWearNote (non-null), and diskWrittenSessionMB inside each workloads[] entry.
3. Generate load: `dd if=/dev/zero of=$TMPDIR/mi_test.bin bs=1m count=2048` while `swift run MonitorIsland --dump` (or a short loop of dumps) — diskWriteBytesPerSec should spike to roughly the dd MB/s. Clean up the temp file.
4. Cross-check the rate against ground truth: `iostat -d -w 1 disk0` and Activity Monitor → Disk (same order of magnitude; iostat combines read+write and can double-count APFS volumes), and two `ioreg -r -c IOBlockStorageDriver -w 0 | grep 'Bytes (Write)'` reads ~2 s apart to hand-compute the delta. NOTE: macmon does NOT track disk — cross-check against iostat, not macmon.
5. Per-process culprit: with the GUI running, do real work in a Claude Code or Codex session (Claude appends to ~/.claude/projects/**/*.jsonl every tool call; Codex writes ~/.codex/*.sqlite). Re-run --dump and confirm the Claude Code / Codex entry's diskWrittenSessionMB climbs.
6. `swift run MonitorIsland --shot out.png` → open the PNG and confirm the Disk rate row, the "SSD~  ~X.XX% est  /  NNN GB life" row, the WRITE ACTIVITY sparkline, and the per-culprit "wrote N MB" sub-lines all render. CRITICAL: the disk rows will be 0 in the shot unless diskWrite/diskRead are in snapToTargets() — verify they render non-zero. To exercise the color escalation, temporarily inject a large lifetime baseline so the SSD~ row crosses 10%/50% (accent→amber→red), then revert.
7. ANTI-SELF-WEAR PROOF: run the GUI ~12 min, then `wc -l` and `wc -c` on ~/Library/Application Support/MonitorIsland/disk_damage.jsonl → expect ~2-3 lines (one per 5-min flush), a few hundred bytes total, NOT one line per tick. Tail it to confirm the schema and that lifetimeWrittenGB is monotonic. Quit with Cmd-Q and confirm a final flush line appended.
8. REBOOT RECONCILIATION (do this, it proves the cumulative requirement): note diskLifetimeWrittenGB, reboot the Mac, relaunch, confirm the figure did NOT reset to ~0 (≥ the pre-reboot value — the boot-counter reset is absorbed by the JSONL-tail baseline).
9. `swift run MonitorIsland --sensors` → unchanged for existing sections; if you added the disk section, confirm it lists all IOBlockStorageDriver entries, the non-zero writer, capacity, and the TBW tier.

=== FINAL INTEGRATION + HONESTY REVIEW PASS (do last) ===
Re-read the full diff and confirm: (a) all hard constraints hold (sudoless, Apple-Silicon-only, no new colors, compact pill untouched, log ≤ once/5 min with the precondition); (b) every estimate is flagged — the SSD~ row shows "~"/"est", diskWearBestEffort is true, diskWearNote is non-null and states the TBW + write-amplification caveat; (c) labels are honest — "host writes" not "NAND writes", lifetime is "since install", per-tool is "session"/"wrote N" using ri_diskio_byteswritten; (d) no NVMe SMART code shipped, just the one-line TODO; (e) the system-wide Disk rate is visually separated from the per-culprit attribution so users don't read the system total as AI-caused. Then produce a short summary: files changed, the exact --dump field names added, and the measured disk_damage.jsonl write cadence from step 7 (to prove the logger is not a wear offender). Do not commit or push unless asked. Report all paths as absolute.